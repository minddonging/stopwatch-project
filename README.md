# JavaScript Stopwatch Project

This project creates a stopwatch on an HTML page using JavaScript.

## Requirements

- Displays time on the page
- Updates every second using `setInterval(functionName, 1000)`
- Includes a button to stop the counter
- Includes Start, Stop, and Reset buttons
